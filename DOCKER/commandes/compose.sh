# Lancer un Docker compose, se placer dans le répertoire où se trouve le fichier docker-compose.yml
docker compose up -d

# Démonter un docker compose (supprimer tous les containers)
docker compose down
