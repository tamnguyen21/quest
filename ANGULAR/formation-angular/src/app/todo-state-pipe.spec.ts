import { TodoStatePipe } from './todo-state-pipe';

describe('TodoStatePipe', () => {
  it('create an instance', () => {
    const pipe = new TodoStatePipe();
    expect(pipe).toBeTruthy();
  });
});
